#
# This module is provided as latticefx_USE_FILE by latticefxConfig.cmake.  It can
# be INCLUDEd in a project to load the needed compiler and linker
# settings to use latticefx.
#

IF(NOT latticefx_USE_FILE_INCLUDED)
  SET(latticefx_USE_FILE_INCLUDED 1)

  # Update CMAKE_MODULE_PATH so includes work.
  #SET (CMAKE_MODULE_PATH
  #  ${CMAKE_MODULE_PATH}
  #  ${latticefx_CMAKE_DIR})

  # Add compiler flags needed to use latticefx.
  #SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${latticefx_REQUIRED_C_FLAGS}")
  #SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${latticefx_REQUIRED_CXX_FLAGS}")
  #SET(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${latticefx_REQUIRED_EXE_LINKER_FLAGS}")
  #SET(CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} ${latticefx_REQUIRED_SHARED_LINKER_FLAGS}")
  #SET(CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} ${latticefx_REQUIRED_MODULE_LINKER_FLAGS}")

  # Add include directories needed to use latticefx.
  INCLUDE_DIRECTORIES(${latticefx_INCLUDE_DIRS})

ENDIF(NOT latticefx_USE_FILE_INCLUDED)
